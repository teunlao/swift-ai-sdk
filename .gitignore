.DS_Store
/.build
/Packages
xcuserdata/
DerivedData/
.swiftpm/configuration/registries.json
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata
.netrc
external/
.dist
dist

# Archive for removed git-tracked files (local backup)
.archive/

# Validation artifacts (entire directory - not tracked in git)
.validation/

# Session contexts (entire directory - not tracked in git)
.sessions/

# Audit files (entire directory - not tracked in git)
.audit/

# Staging and trash (local development)
.staging/
.trash/

# Planning documents (moved to archive)
plan/

# Insights (moved to archive)
insights/

# Parity tracking (moved to archive)
parity/

# Agent and project configuration (moved to archive)
git-local-config.md
AGENTS.md
CLAUDE.md
.claude/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
dev-debug.log
# Dependency directories
node_modules/
# Environment variables
.env
# Editor directories and files
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.next
tsconfig.tsbuildinfo
.build
.build*
# OS specific

# Task Master AI (optional tool, each developer installs if needed)
.taskmaster/

# Orchestrator MCP (database files)
orchestrator.db
orchestrator.db-journal

# Automation artifacts generated by agents
.orchestrator/

# Internal notes and experiments (not for commits)
.info/
worktrees/
.astro
.staging/
