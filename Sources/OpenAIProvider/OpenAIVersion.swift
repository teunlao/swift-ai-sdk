import AISDKProviderUtils

/// Version string for the OpenAI provider package.
/// Mirrors `packages/openai/src/version.ts`.
public let OPENAI_VERSION = SDKReleaseVersion.value

/// Alias matching the upstream package export (`VERSION`).
public let VERSION = OPENAI_VERSION
